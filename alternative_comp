mobius:=function(G)
  local s,i,subG;
  subG:=sub(G);
  s:=Size(subG);
  if Size(subG)=1 then
    return 1;
  elif Size(subG)=2 then
    return -1;
  else
    for i in Reversed([2..s-1]) do
      return -1*(mobius(subG[i])+mobius(subG[i-1]));
    od;
  fi;
end;;
  
  
  
