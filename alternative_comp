mobius:=function(G)
  local s,i,subG;
  subG:=sub(G);
  s:=Size(subG);
  if G=e then
    return 1;
  elif Size(sub(G))=2 then
    return -1;
  else
    for i in Reversed([2...s-1]) do
      return -1*(mobius(subG[i])+mobius(subG[i-1]));
    od;
  fi;
end;;
  
  
  
